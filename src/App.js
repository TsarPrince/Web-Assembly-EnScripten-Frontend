import Login from './components/Login';
import Compiler from './components/Compiler';

function App() {

  return (
    <div>
      <nav>
        <Login />
      </nav>
        <Compiler />
    </div>
  );
}

export default App;
